import * as React from "react";

export type ScrollBarExampleProps = {
    children?: React.ReactNode,
};

export type ScrollBarExampleState = {

};

export const initScrollBarExampleState: ScrollBarExampleState = {

};
